# React + GSAP Interactive Menu

Este projeto é uma exploração de interface de usuário (UI) focada em experiência e fluidez. Utilizando a biblioteca GSAP (GreenSock Animation Platform), criei um menu de navegação que foge do óbvio, transformando uma funcionalidade básica em um elemento de destaque no design.

O objetivo principal foi aplicar conceitos de timelines e easing para criar uma navegação que pareça natural e responsiva ao comando do usuário.

## Demonstração

[Em breve irei adicionar um vídeo mostrando como o menu funciona]

## Como utilizar e rodar localmente

Siga os passos abaixo para configurar o ambiente de desenvolvimento:

### 1. Clonar o repositório

```bash
git clone https://github.com/RyanGabr/gsap-menu.git
cd gsap-menu
```

### 2. Instalar as dependências

Execute o comando abaixo para instalar todas as bibliotecas necessárias listadas no package.json:

```bash
npm install
```

### 3. Iniciar o servidor de desenvolvimento

```bash
npm run dev
```

O projeto será aberto automaticamente em http://localhost:5173.
